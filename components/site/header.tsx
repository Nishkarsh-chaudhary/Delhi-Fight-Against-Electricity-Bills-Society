'use client';
import { Menu, ArrowUpRight, ArrowRight } from 'lucide-react';
import { usePathname } from 'next/navigation';
import { Sheet, SheetTrigger, SheetContent, SheetTitle, SheetDescription, SheetClose } from '@/components/ui/sheet';
import { Button } from '@/components/ui/button';
import { navigation, society } from '@/content/home';
export function Header(){const pathname = usePathname()?.replace(/\/$/, '') || '/'; const isContactPage = pathname === '/contact'; return <><a href="#main" className="skip-link">Skip to content</a><div className="utility"><div className="container"><span>For electricity consumers across Delhi</span><a href="#registration">Reg. No. {society.registration}<ArrowUpRight size={13}/></a></div></div><header className="site-header"><div className="container header-inner"><a href="/" className="identity" aria-label={`${society.name} — home`}><strong>Delhi Fight Against<br/>Electricity Bills Society</strong><span>{society.caption}</span></a><nav className="desktop-nav" aria-label="Main navigation">{navigation.map(([label,href])=><a key={href} href={href} className={pathname === href ? 'active' : undefined} aria-current={pathname === href ? 'page' : undefined}>{label}</a>)}</nav><div className="header-actions">{!isContactPage && <div className="header-cta"><a href="/contact" className="button compact">Submit your issue <ArrowRight size={17}/></a></div>}<Sheet><SheetTrigger asChild><Button variant="ghost" className="mobile-menu" aria-label="Open navigation"><Menu size={24}/></Button></SheetTrigger><SheetContent className="mobile-sheet"><SheetTitle>Explore the homepage</SheetTitle><SheetDescription>{society.name}</SheetDescription><nav aria-label="Mobile navigation">{navigation.map(([label,href])=><SheetClose asChild key={href}><a href={href}>{label}</a></SheetClose>)}{!isContactPage && <SheetClose asChild><a href="/contact" className="button">Submit your issue</a></SheetClose>}</nav></SheetContent></Sheet></div></div></header></>}
