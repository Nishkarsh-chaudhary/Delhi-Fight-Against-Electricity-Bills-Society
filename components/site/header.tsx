'use client';
import { Menu, ArrowUpRight } from 'lucide-react';
import Link from 'next/link';
import { Sheet, SheetTrigger, SheetContent, SheetTitle, SheetDescription, SheetClose } from '@/components/ui/sheet';
import { Button } from '@/components/ui/button';
import { navigation, society, issuePreview } from '@/content/home';
import { InfoDialog } from './info-dialog';
export function Header(){return <><a href="#main" className="skip-link">Skip to content</a><div className="preview-bar">Homepage design preview <span>·</span> Content is under review</div><div className="utility"><div className="container"><span>For electricity consumers across Delhi</span><a href="#registration">Reg. No. {society.registration}<ArrowUpRight size={13}/></a></div></div><header className="site-header"><div className="container header-inner"><Link href="/" className="identity" aria-label={`${society.name} — home`}><strong>Delhi Fight Against<br/>Electricity Bills Society</strong><span>{society.caption}</span></Link><nav className="desktop-nav" aria-label="Main navigation">{navigation.map(([label,href])=><a key={href} href={href}>{label}</a>)}</nav><div className="header-actions"><div className="header-cta"><InfoDialog {...issuePreview} label="Submit your issue" className="button compact"/></div><Sheet><SheetTrigger asChild><Button variant="ghost" className="mobile-menu" aria-label="Open navigation"><Menu size={24}/></Button></SheetTrigger><SheetContent className="mobile-sheet"><SheetTitle>Explore the homepage</SheetTitle><SheetDescription>{society.name}</SheetDescription><nav aria-label="Mobile navigation">{navigation.map(([label,href])=><SheetClose asChild key={href}><a href={href}>{label}</a></SheetClose>)}<SheetClose asChild><a href="#share-concern" className="button">Share your concern</a></SheetClose></nav></SheetContent></Sheet></div></div></header></>}
